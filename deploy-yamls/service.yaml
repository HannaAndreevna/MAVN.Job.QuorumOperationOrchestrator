apiVersion: v1
kind: Service
metadata:
  labels:
    app: quorum-operation-orchestrator
    environment: Development
  name: quorum-operation-orchestrator
  namespace: jobs
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: quorum-operation-orchestrator
  sessionAffinity: None
  type: ClusterIP
