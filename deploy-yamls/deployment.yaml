apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    description: Lykke.Job.QuorumOperationOrchestrator
  labels:
    app: quorum-operation-orchestrator
    environment: Development
    tier: Service
  name: quorum-operation-orchestrator
  namespace: jobs
spec:
  revisionHistoryLimit: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: quorum-operation-orchestrator
  template:
    metadata:
      labels:
        app: quorum-operation-orchestrator
      name: quorum-operation-orchestrator
    spec:
 #     nodeSelector:
 #       type: 
 #     tolerations:
 #     - effect: NoSchedule
 #       operator: Exists
      imagePullSecrets:
      - name: quorum-operation-orchestrator-docker
      containers:
      - name: quorum-operation-orchestrator
        image: lykkebusiness/job-quorumoperationorchestrator:dev
        imagePullPolicy: Always
        lifecycle:
          postStart:
            exec:
             command:
             - /bin/sh
             - -c 
             - "/bin/echo 'options single-request-reopen' >> /etc/resolv.conf"
        ports:
        - containerPort: 5000
        readinessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 20
        resources:
          requests:
            memory: 128Mi 
            cpu: 40m 
          limits:
            memory: 1024Mi 
            cpu: 1000m 
        env:
        - name: BuildNumber
          value: '$version'
        - name: SettingsUrl
          value: '$ConnectionString'
        - name: ENV_INFO
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: APPINSIGHTS_INSTRUMENTATIONKEY
          value: 
        - name: MyMonitoringUrl
          value: http://quorum-operation-orchestrator.jobs.svc.cluster.local/api/isalive
